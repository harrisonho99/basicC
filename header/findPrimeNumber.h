
#define MAX_SIZE 1000
int findPrimeNumber(int start, int end)
{
    int listPrime;
    for (int i = start; i < end; i++)
    {
        switch (start)
        {
        case 1:
            break;
        case 2:
            break;
        case 3:
            break;
        default:
            break;
        }
    }

    return listPrime;
}